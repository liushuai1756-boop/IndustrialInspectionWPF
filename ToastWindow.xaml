<Window x:Class="IndustrialInspectionWPF.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="提示" Height="120" Width="300"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False">

    <Border Background="#333" CornerRadius="10" Opacity="0.9">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.5"/>
        </Border.Effect>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- 加载圈动画 (可选，这里用文字简单代替) -->
            <TextBlock Text="⏳" FontSize="30" HorizontalAlignment="Center" Foreground="White" Margin="0,0,0,10"/>
            <TextBlock x:Name="TxtMsg" Text="指令已发送..." FontSize="16" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" TextAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>